import React from 'react';
import styles from './Spinner.module.css'; // Assuming the file is named Spinner.module.css

const Spinner = () => {
  return <div className={styles.spinner}></div>;
};

export default Spinner;
